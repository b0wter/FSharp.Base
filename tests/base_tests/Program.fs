module BaseTests.Program

[<EntryPoint>]
let main args =
    printfn "This project only contains unit tests. Please run it via `dotnet test`."


    let x = b0wter.FSharp.Validation.ValidationResult.Default<string>.Success "lul" 

    0